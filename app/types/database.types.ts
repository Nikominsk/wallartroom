// Placeholder for the Supabase-generated database type. The @nuxt/supabase
// module looks here by default; without the file it logs a "Using Database =
// unknown" warning at every dev-server start.
//
// To get real typed queries, regenerate via:
//   supabase gen types typescript --project-id <id> > app/types/database.types.ts
// (or use `supabase gen types typescript --local` against a local stack).

export type Database = unknown
